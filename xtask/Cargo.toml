[package]
name = "xtask"
description = "repo automation tasks (not published)"

version = { workspace = true }
edition = { workspace = true }
homepage = { workspace = true }
repository = { workspace = true }
license = { workspace = true }

[dependencies]
cargo_metadata = "0.18"
regex = "1"
